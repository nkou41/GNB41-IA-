name: Garder le backend Render eveille

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping /api/health
        run: curl -fsS https://gnb41-ia.onrender.com/api/health || echo "Ping echoue, le serveur redemarre probablement"
